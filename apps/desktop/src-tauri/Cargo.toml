[package]
name = "nomadfm"
version = "0.1.0"
description = "Nomad FM desktop shell — loads the web app in a native window"
authors = ["Nomad FM"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
